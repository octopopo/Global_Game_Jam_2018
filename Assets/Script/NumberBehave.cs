using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(SpriteRenderer))]

public class NumberBehave : MonoBehaviour {

	// Use this for initialization
	void Start () {
        tag = "Number";
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
